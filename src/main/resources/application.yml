spring:
  datasource:
    url: jdbc:mysql://localhost:3306/anyang?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Seoul
    username: root
    password: joon020712
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
    open-in-view: false

# 🔽 여기를 새로 추가
jwt:
  secret: "CHANGE_ME_TO_A_LONG_RANDOM_BASE64"   # 32바이트 이상 (openssl rand -base64 32)
  expiration: 3600000                           # 액세스 토큰 만료(ms) 예: 1시간
  issuer: "anyang-app"                          # (JwtUtil에서 쓰면)

server:
  port: 8081

logging:
  level:
    org.springframework.security: DEBUG

apis:
  kakao:
    baseUrl: https://dapi.kakao.com
    restKey: 2bcc0b6ff4151cded333601b154a5427   # ← 네 REST API 키
  ftc:
    baseUrl: https://apis.data.go.kr/1130000/FftcIndutyFrcsOpclStatsService  # ← 뒤에 1 빼기
    serviceKey: aXJCcoLEku2k0PufWIaxo2ePRT9rWVVelckIr1J+ssTxkwxL1ICglPeQeb9rlJ/5IDVpgVgscaZj8oHYk7VjTw==
    op: getIndutyFrcsOpclOutStats
