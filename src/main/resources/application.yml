spring:
  datasource:
    url: jdbc:mysql://localhost:3306/helloSpring?useSSL=false&characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: joon020712
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect  # ✅ 이 줄 추가

jwt:
  secret: your_jwt_secret_key_here   # ✅ JWT 키 설정
  expiration: 3600000                # ✅ 토큰 유효시간 (ms 단위, 예: 1시간)
